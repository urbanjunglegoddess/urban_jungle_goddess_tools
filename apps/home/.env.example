# Where each tool is deployed. Set these on THIS project (apps/home) — not on
# the project they point at. Every one is optional: without it the card renders
# with no Open button, which is correct, not broken.
PUBLIC_FIELD_GUIDE_URL=https://your-field-guide-deployment.vercel.app
PUBLIC_FOCUS_URL=https://your-focus-deployment.vercel.app
PUBLIC_LAYOUT_LAB_URL=https://your-layout-lab-deployment.vercel.app
