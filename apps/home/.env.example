# Where the Field Guide is deployed. Without it, the card on the home page
# renders with no Open button — which is correct, not broken.
PUBLIC_FIELD_GUIDE_URL=https://your-field-guide-deployment.vercel.app
