"use client";
import FocusWindow from "./FocusWindow";
/** Tool 3 — combined: selectable compare grid feeds a static timed plan. */
export default function CombinedPlanner() { return <FocusWindow variant="combined" />; }
