// Plain React example (Vite / CRA / any bundler). Copy lib/ and components/ into
// your project and render the tool you want.
import LiveFocusWindow from "./components/LiveFocusWindow";
// or: FitCalculator, Planner, CombinedPlanner

export default function App() {
  return <LiveFocusWindow />;
}
