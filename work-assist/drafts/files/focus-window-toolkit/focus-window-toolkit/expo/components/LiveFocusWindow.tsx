import React from "react";
import FocusWindow from "./FocusWindow";
/** Tool 4 — live: combined + Start / live now-line / hand-off haptic. */
export default function LiveFocusWindow() { return <FocusWindow variant="live" />; }
