"use client";
import FocusWindow from "./FocusWindow";
/** Tool 1 — the calculator: window + compare grid, no plan. */
export default function FitCalculator() { return <FocusWindow variant="fit" />; }
