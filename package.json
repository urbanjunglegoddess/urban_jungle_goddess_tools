{
  "name": "urban-jungle-goddess-tools",
  "version": "0.0.0",
  "private": true,
  "description": "A set of tools in various languages to help run my business.",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "check": "turbo run build test typecheck"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
