{
  "name": "urban-jungle-goddess-tools",
  "private": true,
  "version": "0.0.0",
  "description": "Internal tooling for Urban Jungle Goddess LLC. A container of independent sites, each deployed on its own.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "check": "turbo run typecheck test build blueprint",
    "migrate": "turbo run migrate"
  },
  "devDependencies": {
    "axe-core": "^4.13.0",
    "playwright": "^1.62.1",
    "turbo": "^2.5.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
