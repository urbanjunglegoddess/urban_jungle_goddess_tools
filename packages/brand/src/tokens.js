/**
 * @ujg/brand — the brand anchors as data, for anything that needs a hex in JS
 * (canvas, meta theme-color, chart series). The CSS custom properties in
 * tokens.css are the source of truth for anything rendered as CSS.
 */
export const ujgBrand = {
  night: "#0A0A0A",
  darkGreen: "#042D1D",
  eminence: "#5F2C82",
  spanishOrange: "#E86100",
  goldenrod: "#DCA424",
  platinum: "#E8E6E1",
  sienna: "#7E3209",
};

export const ujgFonts = {
  display: '"Chakra Petch", "Urbanist", ui-sans-serif, system-ui, sans-serif',
  body: '"Urbanist", ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif',
  mono: '"JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace',
};
